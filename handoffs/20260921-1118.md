# 交接筆記

最後更新：2026-09-21 11:18 UTC+08:00
交接原因：可移交里程碑（人工逐局移除＋VideoEvidence schema 已完成但未提交）

## 目前目標

先審查並發布工作區中的第一批改動；完成前不要開始 Gemini／YouTube API 管線。

本批已在工作區移除 Compare 的人工新增、刪除與匿名匯出流程，刪除 `BattleRoundRecord`
全鏈路，將 IndexedDB 升至 v5 並只移除 `battleRounds`，舊備份逐局欄位會被忽略；另新增
唯讀 `VideoEvidenceDataset` schema、fixture、runtime 驗證與品質閘門測試。

## 發布狀態

| 層級 | 狀態 |
|---|---|
| 工作區 | `e699631` 加未提交功能改動；另有開工前已存在的 `CLAUDE.md` 使用者修改 |
| `origin/main` | `e699631` |
| 線上 Pages | 本輪未部署、未跑 live test；上一份交接記載 `7fbaf1a`，本輪未重新確認 |

使用者明確要求本輪不要 commit 或 deploy，因此工作區刻意保持未提交。

## 已驗證與未驗證

- `npx tsc -b`：通過。
- `npm test`：24 files / 435 tests 通過。
- `npm run build`：通過；僅既有 500 kB chunk 警告。
- `npx playwright test --workers=1`：87 passed / 1 skipped；skip 是既有手機專案 WebKit 離線限制。
- `npm run shots`：2 passed；已目視 phone／desktop 的 `compare-filled`，人工逐局區塊消失且版面正常。
- `git diff --check`：通過。
- 未驗證：commit、push、Pages deploy、`npm run test:live`。

## 阻塞

- 首次真實影片回填仍需要 `GEMINI_API_KEY`、`YOUTUBE_API_KEY`。
- 全球可信頻道白名單尚未建立，不能把自動搜尋結果直接視為可信來源。

## 下一個具體動作

主 session 先重讀完整 diff 與本交接，確認是否接受目前 VideoEvidence v1 邊界；若接受，
再依使用者授權決定 commit／push／deploy。未發布這批前不要開始下一個大型功能。

## 怎麼跑（非顯而易見的）

- E2E 這台機器以單 worker 才可重現全綠；設定已改成 `workers: 1`。
- 完整發布順序仍是 `npm run test:e2e` → `npm run shots` 並看圖 → push →
  `npm run deploy:pages` → `npm run test:live`。
- migration integration test 會真的建立 v4 DB，再用目前 schema 升至 v5。

## 踩過的坑

- 本輪 4 workers 出現 9 條 DOM 已載入但操作逾時；2 workers 仍有 1 條；單 worker
  87 passed / 1 skipped。不要把平行負載逾時誤判成 v5 migration 回歸，也不要用 retry 掩蓋。
- `battleRounds` 在 production 只應出現在 v4 歷史 schema 與 v5 的 `null` 刪表宣告；
  兩行不能刪，否則既有裝置無法完成定向 migration。
- fullPage 截圖會把 fixed 導覽列畫在頁面中段；本輪 phone 圖的中段導覽列是既有截圖假象。

## 已知缺口

- VideoEvidence 尚未接前台證據頁、真實資料產物或模型；本批只有資料契約與驗證邊界。
- schema 的「完整配置識別」只做無 Catalog 時可判定的最低檢查；合法組合與完整槽位需在
  後續 Catalog 映射閘門驗證。
- 自動發現、白名單核准、歷史回填、增量游標、重試／續跑、模型訓練與品質報告尚未實作。
- 線上仍是舊版，因本輪明確禁止 commit／deploy。

## 下一步（排序）

1. 發布本批，先讓線上人工逐局入口消失並執行 live smoke。
2. 建立可信來源 registry 與核准格式；先定來源邊界才能安全自動發現。
3. 實作 YouTube 清單／游標與固定 Gemini response fixture 的兩階段抽取。
4. 加入 Catalog 合法性、雙次抽取一致性與信心閘門，再產出高／低信心證據。
5. 樣本達門檻後才訓練、評估與接 Compare／3on3。

## 資料管線表

`可信頻道 registry → YouTube Data API → Gemini 分段／逐局抽取 → VideoEvidence validator →
Catalog 映射與信心閘門 → 高／低信心 JSON → 高信心訓練集 → 模型／品質報告 → Compare／3on3`

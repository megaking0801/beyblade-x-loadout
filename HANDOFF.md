# 交接筆記

最後更新：2026-09-18。線上版：<https://megaking0801.github.io/beyblade-x-loadout/>

這份寫給明天接手的人（含未來的自己）。只寫「現在長什麼樣、哪裡還是壞的、下一步怎麼做」，
不重複 README 已經有的東西。

---

## 一分鐘現況

| 項目 | 狀態 |
|---|---|
| 單元＋整合測試 | 378 passed（17 檔） |
| E2E | 49 passed、1 skipped（WebKit 離線 reload 是 Playwright 驅動限制，測試檔內有註明解除條件） |
| 線上煙霧測試 | `npm run test:live` 4 passed |
| `tsc -b` | 無錯誤 |
| 已部署 | main 全部已推，gh-pages 與 main 同步 |

### 怎麼跑

```bash
npm install
npm run dev              # 本機開發
npm test                 # 單元＋整合測試
npx playwright test      # E2E（先確認 4173 沒有殘留的 preview 佔用）
npm run test:live        # 打線上版的煙霧測試
npm run build:catalog    # 重建圖鑑 JSON（改過 scripts/ 之後一定要跑）
npm run deploy:pages     # 建置 + 推 gh-pages（推 code 就是要上線一版）
```

**踩過兩次的坑**：`playwright.config.ts` 的 `reuseExistingServer` 會沿用 4173 上既有的
server。如果之前手動開過 `npm run preview` 沒關掉，E2E 會拿**舊的 dist** 去跑，
結果可能整批假綠或假紅。跑 E2E 前先清掉 4173。

**另一個坑**：`匯出備份與匯入備份可以來回` 這條 E2E 在整檔一起跑時要十幾秒。
實測 `repo.importBackup` 只花 5 ms，慢的是 Playwright 經 CDP 把檔案交給 Chromium
（整檔跑時同時在錄 trace）。超時已放寬到 30 秒並在測試檔註明，不要再當成產品效能問題去追。

---

## 這一輪第四段（2026-09-17）又做了什麼

### 11. 圖片改成本機副本（原待辦 F，由專案擁有者決定）

原本全部熱連結，來源改路徑或擋掉就整批破圖。擁有者決定自存一份：

- `npm run fetch:images` 下載到 `public/img/`，檔名是網址雜湊（來源改版會自然變成新檔名）
- macOS 的 `sips` 縮到長邊 320px（顯示最大只到 72px）：**36 MB → 8.9 MB**，最大單張 76 KB
- service worker 把 `img/**` 排除在預快取之外（否則安裝包爆掉），改走 CacheFirst；
  預快取維持 795 KB
- 本機路徑要經 `assetUrl()` 補上 `BASE_URL`，不然 Pages 子路徑會 404

**誠實界線（重要）**：鏡像不等於取得授權。
`usageStatus` 標 `unknown`（授權狀態未確認），不是 `permission_granted`；
`remoteUrl`、`sourceUrl`、`sourceName` 全部保留，零件與商品詳情頁顯示
「本機副本（未取得授權）」與原始位置。測試會擋住把它寫成已授權。

**站是公開的**（GitHub Pages 任何人可開），所以自存等於對外散布，不只是自己用；
這是擁有者的決定，需要改回熱連結的話把 `images.local.json` 刪掉重新產生圖鑑即可。

### 12. 配裝器／3on3／比較三頁版面重排（原待辦 E）

三頁原本是一長條卡片疊下來，桌機右邊一大片空白、結果要滑很久才看到。

- 新增 `.work-split`：桌機分兩欄（左操作、右結果），右欄 sticky 跟著捲動；手機仍單欄
- 新增 `.compare-table`（勝出側加底色）、`.deck-member`（角色標籤＋配裝名＋理由）、
  `.stack`、`.chip-row`
- 三頁的 inline style 大幅減少（配裝器 31 → 27 處剩下的是真的一次性尺寸）
- 比較頁沒選滿兩套時右欄顯示引導，不再一片空白
- 補 e2e：選兩套之後第 34 節要求的八個比較項目都要出現

---

## 這一輪第三段（2026-09-17）又做了什麼

### 7. CX 的鎖定紋章與主刃拆開了（原待辦 A 完成）

社群資料檔裡本來就有完整的 CX 零件清單，用「紋章中文名 + 主刃中文名 == 合併件中文名」比對：

- **12 顆拆成紋章 + 主刃**，可以混搭不同商品的零件（這是 CX 系統的賣點，之前做不到）
- **4 顆維持合併**：雄鹿之角、海怪扭動、大黃蜂堡、龍神勇氣（社群站還沒收錄它們的紋章／主刃）
- 金屬主刃用獨立 id（`main_blade:metal-Fr`）：普通主刃「閃焰」與金屬主刃「堡壘」代號都是 Fr，
  只靠 `family:code` 會撞號、後者會拿到前者的資料
- 拆出來的零件身分標 `community_only`，因為官方只公布合併後的名稱

**資料遷移**：`partIdMigrations` 記舊 id → 新 id，`repository.loadCatalog` 會把裝置上的
零件批次（一筆拆成兩筆）、已存配裝（主刃槽拆成鎖定紋章 + 主刃）與零件收藏搬過去，
搬完寫一筆紀錄到 meta。只有「舊 id 在新圖鑑真的不存在」才搬，重複載入不會重複拆。
整合測試有守：拆批次、狀態與備註保留、配裝槽位改寫、重複載入不重複搬、新 id 不存在時不搬。

### 8. 固鎖一體型零件

UX 的「擴張上蓋」與 Op／Tr 這類軸心把固鎖做在自己身上，商品名裡就沒有固鎖段
（バレットグリフォン「H」、ペガサスブラスト「A」「Tr」），以前一律判成「解析不出零件組成」。

現在 `Part.integratedRatchet` 標記這類零件，選到它之後固鎖欄位會消失、硬選固鎖會被擋。
哪些零件是一體型官方不會標，記在 `src/catalog/sources/beybladehub-structure.json`（社群來源）。

### 9. 套裝內容全部補齊（原待辦 B、C 完成）

自動抓取處理不了兩類：多顆套裝（商品頁錨點混了「推薦搭配」池，照抄會多列一堆不在盒內的零件）、
BX-00／UX-00／CX-00 無編號商品（社群站網址不是用型號組的，抓取一律 404；
它們用 `/parts/combos/{系列}-00-{識別碼}`，25 週年組甚至是 `BXG-00`）。

改成逐筆讀商品頁、人工彙整到 `src/catalog/sources/beybladehub-curated-sets.json`，
**每筆都附商品頁網址與原文引述**，可回頭核對。10 筆套裝、26 顆陀螺。

結果：**內容未知 0 筆、零件組成解析不出 0 筆**。除了隨機補充包（刻意不填）與配件（本來就沒零件），
每個商品都有內容了。只出現在套裝內的上蓋（戰羊阻截、鮫鯊撕裂、惡魔幽冥、飛龍颶風、
勝利戰神、暴龍、滄龍、棘龍、風神翼龍、2.0 版上蓋）身分標 `community_only`。

### 10. 圖鑑模式效能再一次守住

CX 拆開後組合空間從 33 萬暴增到約 330 萬，便宜評分也要 30 秒。
現在超過 15 萬組就先依排序軸把各部位取前段再枚舉，並回報已截斷；實測回到約 1 秒。

---

## 這一輪後段（2026-09-17）又做了什麼

### 5. 稽核把「刻意不填」與「真的缺」分開（原待辦 B 的清理）

原本「內容待查 30 筆」把三種東西混在一起，看起來缺漏是實際的三倍：

| 分類 | 筆數 | 說明 |
|---|---|---|
| 真的還缺 | 9 | 套裝內容未公布（＋8 筆商品名解析不出零件） |
| 刻意不填 | 21 | 隨機補充包，開封後由使用者登記 |
| 本來就沒零件 | 22 | 發射器、對戰盤、握把、收納盒 |

`fetchHubSetContents.mjs` 也不再把配件類當成待補目標 —— 以前會白抓 29 次 404，
讓 skipped 看起來像資料缺漏，現在只剩 8 筆真的沒有商品頁。設定頁照這三類分開顯示。

### 6. 圖片政策落實（原待辦 F 的可做部分）

圖片全部只做外部連結（第 25 節），這一輪把「只連結」這件事做到位：

- **新增 `npm run check:images`**：實際抓每一張圖確認還取得到。
  現況 280 張全部可取得（BeybladeHub 192、Takara Tomy 61、Rakuten 26、Amazon 1）。
  以前的檢查會漏掉 135 張零件圖，因為它讀的是中間產物 `images.generated.json`。
- **修掉錯誤的版權歸屬**：去背零件圖原本標 `copyrightOwner: 'BeybladeHub'`，
  但商品外觀著作權仍屬 Takara Tomy，社群站只是去背整理。現在不知道版權人就留空，
  `sourceUrl` 也改指向實際取圖的 BeybladeHub 頁面（原本指向 Takara Tomy 商品頁，對不上）。
- **離線快取補上 `img.beybladehub.app`**：runtimeCaching 原本只列官方與電商網域，
  漏掉零件去背圖所在的網域，等於零件頁離線全破。
- **UI 看得到來源**：零件與商品詳情頁顯示「圖片來源 + 版權人 + 授權狀態」，
  設定頁新增「圖片來源」區塊列出各來源張數。

---

## 這一輪前段（2026-09-17）做了什麼

### 1. CX 四件式（超越拆組）建模完成 — 上一輪的待辦 A 已解

`PartFamily` 加了 `over_blade`（超越戰刃），CX 槽位變成
鎖定紋章＋主刃＋超越戰刃＋輔助戰刃＋固鎖＋軸心，並依主刃是三件式或四件式自動收放欄位。

- 官方商品名把兩片黏在一起寫：`バハムートブリッツ「BK」` = 超越 `B` + 輔助 `K`。
  `buildCatalog.mjs` 的 `CX_BEY` 現在會拆開，四件式主刃標 `cxOverBlade`。
- 假的兩字母輔助戰刃 `BK`／`GV`／`FE`／`FT` 已消失，改成正確的
  `over_blade:B/G/F` + `assist_blade:K/V/E/T`。
- 「某支商品是三件式還是四件式」官方沒公布，來源是 BeybladeHub 商品頁，
  已**逐筆確認** CX-01 三件式、CX-13／CX-14／CX-15 四件式，所以拆出來的零件標 `community_only`。
- CX-16 的內容物現在補得出來，而且含超越戰刃（之前抓取腳本只認三個槽位，會漏一片）。
- 結構文案改成「CX 模組化（上蓋三件式／四件式）」，原本寫死「五件式」對四件式是錯的。

### 2. 修掉三個真的壞掉的東西

- **配裝器的 CX 模式根本沒法用**：槽位表是由 `slots` 推導，剛切到 CX 時 slots 是空的，
  `deriveSystem` 會推成 BX，所以 CX 欄位永遠不出現。改成由使用者選的結構直接決定
  （`getCxSlotSchema`），並補 E2E 守這條。
- **「我能組什麼」的圖鑑模式會凍住畫面**：每個組合都跑完整 `analyzeCombo`，
  實測 28 秒。改成兩段式——先用同一組公式做便宜評分排序，只對前段做完整分析——
  變成 1.4 秒。`tests/unit/builderPerf.test.ts` 守 5 秒上限。
  同時修好 CX 從來不被枚舉的問題（舊碼要求有 `lock_chip` 零件，但圖鑑全是 `cxFused`）。
- **`fetchHubSetContents.mjs` 會抹掉自己的成果**：它只抓「內容為空」的商品，
  但輸出是整檔覆寫，所以上一輪補齊的 CX-04 這一輪不是 target、直接消失。
  改成與既有檔案合併。（CX-04 已從 git 取回。）

### 3. 前台不再把社群數值講成官方數值

資料層早就把數值分開記在 `Part.statsProvenance`（`community_only`），
但畫面上仍寫「官方重量」「官方類型」。新增 `src/domain/provenance.ts`：

- `statFieldLabel`：只有官方來源才冠「官方」，社群來源標「（社群實測）」
- 零件詳情頁的來源區塊拆成「零件身分」與「類型、重量與軸心特性」兩段，各自列來源與核對日期
- 配裝結果帶出社群數值提醒與來源連結（第 22 節：UI 必須可查看來源）

### 4. 其他

- 匯入備份改成匯入成功就先回饋，再重讀全域狀態（原本要等整份圖鑑重載完才顯示訊息）
- 零件列表的白話說明夾成兩行（社群說明有些是一整段，會把卡片擠爆）
- 外層加 `data-route`，E2E 的 `openApp` 等到真的換頁才操作
- 旋向中文表集中到 `types.ts` 的 `SPIN_DIRECTION_ZH`
- 新增 `npm run test:live` 與 `scripts/deployPages.mjs`（git worktree 推 gh-pages）

---

## 已知還是壞的 / 缺的

### A. 4 顆 CX 上蓋仍是合併的

雄鹿之角、海怪扭動、大黃蜂堡、龍神勇氣。社群站零件頁還沒收錄它們的紋章與主刃，
所以維持合併並標 `cxFused`（配裝時不會出現鎖定紋章欄位）。
等社群站補上就能自動拆開：`buildCatalog.mjs` 的拆解是比對中文名，不需要改程式。

### B. 套裝內容已補齊，但維護方式是人工

`beybladehub-curated-sets.json` 是手工彙整的，社群站改版或新商品上架時要手動補。
自動抓取（`fetchHubSetContents.mjs`）只處理得了「錨點剛好就是內容」的單純情況。
要自動化得先解決「怎麼區分內容與推薦搭配池」。

### C. 3 筆商品的官方名稱仍解析不出零件，但內容已由彙整補上

`bxa02`、`bx00-jtm`、`bx00-jsq`（亞洲錦標賽紀念組、兩款侏羅紀世界雙入門組）。
名稱裡沒有固鎖代號也不是一體型，所以走人工彙整。

### D. 重量幾乎全缺（原本這條低估了，實際核對過）

不只是上蓋。**除了上蓋以外，沒有任何零件有公克數**：

| 零件 | 數量 | 缺重量 |
|---|---|---|
| 上蓋 | 71 | 24 |
| 固鎖 | 32 | 32（全缺） |
| 軸心 | 48 | 48（全缺） |
| 鎖定紋章 | 17 | 17（全缺） |
| 輔助戰刃 | 15 | 15（全缺） |
| 超越戰刃 | 3 | 3（全缺） |
| 主刃 | 16 | 16（全缺） |

BeybladeHub 的固鎖與軸心卡片寫的是相對記號（`6+`、`2=`）不是公克，所以抓不到。
影響：總重永遠算不出來，**每一套配裝的可信度上限都卡在「中等」**。
缺重量的 24 顆上蓋多半是聯名款、金屬塗裝復刻與 2.0 版。

要補得另找來源（WBO 論壇實測串，或自己秤）。這是目前唯一會實際削弱功能的缺口。

### E. 三頁版面已重排，但仍有小瑕疵

桌機兩欄、sticky 結果欄都做好了。剩下的小問題：
3on3 與比較頁在桌機的左欄只有一個下拉選單，下面留白偏多；
手機版三頁的區塊間距偏鬆（Section 18px + stack 12px）。

### F. 圖片是本機副本，但沒有授權

已自存一份（見第 11 項），破圖風險解除，但**授權狀態仍是未確認**。
真要洗乾淨只有兩條路：向 BeybladeHub／Takara Tomy 取得同意後把 `usageStatus`
改成 `permission_granted`，或改用官方素材（官方只有整盒包裝照，沒有單件去背圖）。

另外：`public/img` 有 9 MB 進了 git，以後每次重抓都會再長一份歷史。
要控制 repo 大小的話得考慮改成部署時才抓。

### G. 小瑕疵：共用代號的零件來源標記取決於誰先建立

例如輔助戰刃 `T` 是先由四件式拆分建立的，所以標 `community_only`；
若之後有三件式商品也用 `T`，`ensurePart` 會沿用既有那顆，來源標記就偏保守（標成社群）。
不影響正確性，但要精確就得讓 provenance 可以合併升級。

---

## 建議的下一步順序

1. **D**：補聯名款與新款上蓋的重量（社群站卡片上沒有，可能要另找來源）。
   這是強度分析可信度上限卡在「中等」的主因。
2. 桌機左欄留白與手機間距的微調（E 的剩料）。
3. `public/img` 的 9 MB 要不要留在 git（F 的剩料）。
4. 自動化套裝內容彙整（需要先想清楚怎麼分辨內容與推薦搭配池）。

---

## 給接手者的提醒

> 完整的長期規則寫在 `CLAUDE.md`，那份會被每個在這個 repo 工作的代理自動讀到。

- **收尾規則：每次交接前一定要更新這份 `HANDOFF.md`、push、再 `npm run deploy:pages`。**
  而且要**一路預留額度**做這三件事 —— 不要把 session 的餘裕全用在寫功能上，
  感覺快到上限就停下來收尾。未推的 commit 與沒更新的 handoff，對接手的人等於不存在。
- **推 code 就是要上線一版。** push 之後接 `npm run deploy:pages`，不要停在「要不要推？」。
  推完跑 `npm run test:live` 確認線上真的換版了（看 `index-*.js` 雜湊有沒有變）。
- 名稱與用語一律以 <https://beybladehub.app> 為準，不要自己翻譯。
  詞素表在 `scripts/zhTwTokens.mjs`，語序修正在 `scripts/buildCatalog.mjs` 的 `ZH_TW_PHRASE_RULES`。
- 該站把「防禦」寫成**防守**。抓類型時兩種都要收，只收一種會讓整批防禦型零件沒有類型。
- 規格第 1.5 節：**不得編造**。查不到就留空、標來源，不要填一個看起來合理的值。
  這一輪就抓到「資料層標社群、畫面上卻寫官方」這種違規，測試現在會守。
- 前台不得出現日文假名（第 1.4 節）。`tests/e2e/pwa.spec.ts` 有一條會巡所有列表頁檢查，
  判斷式刻意排除全形中點「・」，不要把它加回去（會整批假警報）。
- 新增的測試若可能「必定通過」，做一次變異驗證（故意弄壞，確認它會紅）。

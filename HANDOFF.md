# 交接筆記

最後更新：2026-09-17。線上版：<https://megaking0801.github.io/beyblade-x-loadout/>

這份寫給明天接手的人（含未來的自己）。只寫「現在長什麼樣、哪裡還是壞的、下一步怎麼做」，
不重複 README 已經有的東西。

---

## 一分鐘現況

| 項目 | 狀態 |
|---|---|
| 單元測試 | 303 passed |
| E2E | 37 passed（1 筆行動版離線測試依原設定 skip） |
| `tsc -b` | 無錯誤 |
| 已部署 | `f25645a` 之後的變更**尚未部署**，見下方「未推上去的東西」 |

### 怎麼跑

```bash
npm install
npm run dev              # 本機開發
npm test                 # 單元測試
npx playwright test      # E2E（先確認 4173 沒有殘留的 preview 佔用）
npm run build:catalog    # 重建圖鑑 JSON（改過 scripts/ 之後一定要跑）
npm run deploy:pages     # 建置 + 推 gh-pages（推 code 就是要上線一版）
```

**踩過兩次的坑**：`playwright.config.ts` 的 `reuseExistingServer` 會沿用 4173 上既有的
server。如果之前手動開過 `npm run preview` 沒關掉，E2E 會拿**舊的 dist** 去跑，
結果可能整批假綠或假紅。跑 E2E 前先清掉：

```bash
for P in $(netstat -ano | grep ":4173" | grep LISTENING | awk '{print $5}' | sort -u); do taskkill //F //PID $P; done
```

---

## 未推上去的東西（明天第一件事）

工作目錄有一批**已完成且測試全過、但還沒 commit** 的改動。內容見下方「這一輪做了什麼」
的第 1～5 項。確認無誤後 commit + push + `npm run deploy:pages`。

新增檔案：

- `scripts/fetchHubStats.mjs`
- `scripts/fetchHubSetContents.mjs`
- `src/catalog/sources/beybladehub-stats.json`
- `src/catalog/sources/beybladehub-sets.json`

---

## 這一輪做了什麼

### 1. 零件數值：配裝分析不再整片「資料不足」

官方商品頁不公布零件的重量、類型、旋向，所以強度分析原本每一項都是「資料不足」，
功能等於不存在。改成從 BeybladeHub 抓玩家實測值。

| 零件 | 筆數 | 類型 | 重量 | 旋向 | 圖片 |
|---|---|---|---|---|---|
| 上蓋 | 60 | 60 | 42 | 60 | 60 |
| 固鎖 | 31 | 31 | 0 | — | 31 |
| 軸心 | 44 | 44 | 0 | — | 44 |
| CX 主刃 | 15 | 0 | 0 | 0 | 0 |
| 輔助戰刃 | 12 | 0 | 0 | 0 | 0 |

誠實界線：零件**身分**（型號、名稱）仍是 `official_verified`；**數值**獨立記在
`Part.statsProvenance`，標 `community_only`。兩者刻意不混為一談，
`tests/unit/catalog.test.ts` 有守這條規則。

### 2. 旋向與可信度的判定改對

- 舊行為：每個沒有旋向的零件都跳一條警告。但旋向由上蓋決定，固鎖左右通用，
  軸心只有標 L 的左旋專用款才有旋向 —— 原本等於每套配裝都吐兩條噪音。
  現在只有「整組都查不到旋向」才提醒。
- 舊行為：缺任何欄位一律判低可信度。固鎖與軸心沒有公開公克數，總重永遠算不出來，
  所以每套都是「低」，這個欄位失去分辨力。現在只缺重量降到「中等」，
  連類型或高度都沒有才是「低」。

### 3. 圖片改用單件去背圖

官方商品圖是整盒包裝照（常常還是背面），手機上縮成 44px 看不出是哪顆。
改用 BeybladeHub 的單件去背圖：零件圖 135 張，只含一顆陀螺的商品也換成該上蓋的圖（57 筆）。
沿用第 25 節 `link_only`：只連結、不下載也不重新散布。

### 4. 加入商品／零件有回饋了

原本按「加入我的商品」只呼叫 `navigate('/products')`，但使用者本來就在該頁，
畫面完全沒變化，所以會重複點、庫存多算。現在改成留在原地，按鈕文字變
「已加入 ×N」約 1.6 秒，卡片外框閃一下硃紅（`useJustAdded`，`.just-added`）。
動畫被系統關掉時文字與外框顏色仍然會變。

### 5. 商品庫加上 BX / UX / CX 快篩

搜尋框上方一排產品線按鈕，帶各線數量。testid 為 `line-filter-{all,BX,UX,CX}`。

### 更早已部署的部分

`4ce8eb2`～`f25645a`：台灣譯名全面比對 BeybladeHub、視覺改版成零件型錄風格、
Service Worker 立即接管、首頁建置戳記與「重新載入最新版」按鈕、四邊 safe area、
手機寬度爆版守門測試。

---

## 已知還是壞的 / 缺的

### A. CX-16 內容物顯示不出來（使用者直接問過）

**根因是資料模型，不是抓不到資料。** BeybladeHub 的 CX-16 頁面清楚列出內容：
鎖定紋章 `Bh`(龍王) + 金屬主刃 `Bl`(閃擊) + **超越戰刃 `B`(破裂)** + 輔助戰刃 `K` + 固鎖 `1-50` + 軸心 `I`。

問題出在本專案把 CX 上蓋建模成「`main_blade`（紋章+主刃合併）+ `assist_blade`」兩件，
**沒有「超越戰刃」這個 family**。所以商品名 `バハムートブリッツBK1-50I` 裡的 `BK`
被 `buildCatalog.mjs` 的 `CX_BEY` 正規表達式整段當成一個輔助戰刃 `BK`，
但實際上是 `B`(超越) + `K`(輔助) 兩件。

於是 `fetchHubSetContents.mjs` 解出 `assist_blade:K`，查不到這個 id，整筆判定為無法對應而略過。

同樣被這個 bug 影響的還有 `assist_blade` 裡的 `BK`、`GV`、`FE`、`FT`（都是兩字母，
八成全部都是「超越+輔助」被黏在一起）。

**要修就得動資料模型**：`PartFamily` 加 `over_blade`、CX 槽位定義加一格、
`CX_BEY` 正規表達式改成能拆出超越戰刃、相容性檢查與相關測試跟著改。
估計是一整個 session 的工作量，不是順手改。

### B. 套裝內容仍有 30 筆未知

`fetchHubSetContents.mjs` 只補齊 1 筆（CX-04）。其餘：

- **Random Booster**：刻意不補。商品頁上的零件連結是「可能抽到的池」，不是盒內固定內容，
  照抄會變成憑空宣告開到什麼。這是正確行為，不要改。
- **配件類**（發射器、對戰盤、收納盒）：BeybladeHub 沒有對應商品頁（404）。
  這些本來就不含零件，目前 audit 把它們歸在「內容未知」，其實應該另分一類「無零件商品」，
  免得看起來像資料缺漏。**這是個小的、值得做的清理。**
- **多顆套裝**（BX-46、BX-53、UX-21 等）：頁面有列零件，但有些零件本專案的圖鑑沒收錄
  （官方一覽頁沒有單獨列出），所以整筆判為不可解。要補就得允許從社群來源新增零件，
  那會鬆動「零件身分只採信官方」這條規則，需要先決定要不要放寬。

### C. 商品名解析不出零件組成：8 筆

`bxa02`、`cx07`、`bx00-jtm`、`bx00-jsq`、`cx00-pb`、`ux19`、`ux20`、`ux00-gv`。
多半是聯名雙顆組與命名特殊的款（`ペガサスブラストATr`、`グローリーワルキューレLF`）。
`ux19`／`ux20` 的失敗原因跟 A 是同一個：CX 命名拆不開。

### D. 上蓋重量只有 42/60

BeybladeHub 卡片上沒有重量的那 18 筆多半是聯名款。影響：總重永遠算不出來，
配裝的可信度上限停在「中等」。

### E. 三個頁面還沒吃到新版面

`BuilderPage`、`DecksPage`、`ComparePage` 仍是大量 inline style。
我保留了舊 CSS 變數名當別名，所以它們拿得到新的色彩與字體，
但**版面結構沒重排**（卡片仍是一坨、桌機沒有分欄）。

### F. 圖片全部是熱連結 BeybladeHub 的 CDN

`img.beybladehub.app`。對方沒有承諾提供這個用途，哪天擋掉或改路徑就會整批破圖
（`PartThumb` 有 `onError` 退回型號佔位圖，不會壞版）。
若要長久，得談授權或自己存一份。**這件事需要你決定，我不會自作主張。**

---

## 建議的下一步順序

1. **先把工作目錄的改動 commit + 部署**，讓手機上看得到（上面「未推上去的東西」）。
2. **A：CX 超越戰刃建模**。這是目前最大的一塊，解掉會連帶修好 CX-16 內容、
   `ux19`／`ux20` 解析、以及 `assist_blade` 的 BK/GV/FE/FT 錯誤。
3. **B 的配件分類清理**：把「無零件商品」從「內容未知」拆出來，小改動、可見度高。
4. **E：三頁的版面重排**。
5. **F：圖片來源決策**。

---

## 給接手者的提醒

- **推 code 就是要上線一版。** push 之後接 `npm run deploy:pages`，不要停在「要不要推？」。
- 名稱與用語一律以 <https://beybladehub.app> 為準，不要自己翻譯。
  詞素表在 `scripts/zhTwTokens.mjs`，語序修正在 `scripts/buildCatalog.mjs` 的 `ZH_TW_PHRASE_RULES`。
- 該站把「防禦」寫成**防守**。抓類型時兩種都要收，只收一種會讓整批防禦型零件沒有類型。
- 規格第 1.5 節：**不得編造**。查不到就留空、標來源，不要填一個看起來合理的值。
  現有測試有在守這條，別為了讓測試變綠而放寬它。
- 新增的測試若可能「必定通過」，做一次變異驗證（故意弄壞，確認它會紅）。
  手機寬度那條測試就是這樣驗過的。
